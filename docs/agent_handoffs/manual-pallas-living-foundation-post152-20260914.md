# PALLAS Living Foundation — post-#152 current-base handoff

## Scope

Fresh reconstruction of the independently qualified PALLAS living-field foundation on exact current Develop base `90f5439bfdb4502bc689c51b06f83586c50c9d7c`, after PR #152 restored Core-Focused type-change coverage.

The five functional code/test/design-document blobs are byte-identical to qualified PR #151 head `b7844cc7c38a9a9b263edaa9b3f7b942a984c1c3`. This reconstruction changes only ancestry and this handoff so the foundation is re-qualified under the stronger current gate.

## Included contracts

- deterministic bounded living/force simulation;
- lexical semantic attraction affects layout only and never graph truth;
- explicit contradiction repulsion;
- vitality diffusion only across real persisted/grounded graph edges;
- runtime age progression `· : + o O ░ ▒ ▓ █`;
- state-preserving snapshot reconciliation;
- Qt bridge over already-grounded rendered nodes/edges;
- compact/full field controller compatibility;
- focused engine and real-controller Qt regression coverage;
- living-field design documentation.

## Excluded boundary

`src/athena/desktop/pathena_pallas_full_view.py` remains excluded. The UI worker owns shell hosting/focus restoration and is reconciled separately in helper PR #153. Do not restore the obsolete detached-dialog implementation.

## Prior evidence

Source PR #151 exact head `b7844cc7c38a9a9b263edaa9b3f7b942a984c1c3` passed:

- pATHENA UI Focused Candidate `34825376994`;
- canonical ATHENA Quality Gate `34825376845`.

Those results establish the functional blob set. This branch still requires fresh exact-head qualification because its parent is the newer post-#152 Develop lineage.

## Integration rule

Require fresh exact-head UI Focused plus canonical Quality and a final collision/drift check. No Skip/XFail, no provenance weakening, no detached-dialog restoration, no visual MATCH claim, no threshold relaxation, no auto-merge.
