# pATHENA UI Handoff

## Current baseline — 2026-09-11

- Run-start Develop: `develop/pathena-next@69b16347bd4bab875c31b7a41830c6bab6a0bb7b`.
- Exact canonical ATHENA Quality Gate: `34576900899 = SUCCESS`.
- Run-start UI worker: `postmerge/ui@69773c189cabca3400f66101934b82b0d80bb38e`.
- `main` and `bnbgrs/ATHENA` remained READ-ONLY and untouched.
- Current Spec/Core, Backend, Errors, Integrator, 11-screen manifest and Visual Gap Ledger were consumed before mutation.

## Fresh 11-screen evidence

The canonical user Library folder contains exactly eleven reference images. All eleven were opened directly in this run. The exact Windows/PySide6 visual artifact for `69773c189cabca3400f66101934b82b0d80bb38e` was downloaded and all eleven current runtime PNGs were opened as well.

The prior Knowledge + Research slice is visibly present in the exact current runtime: Knowledge shows `KNOWLEDGE / NONE` with Provenance/Evidence guidance; Research shows `RESEARCH / NONE` with Synthesis/Evidence guidance. Jobs and Settings retain their truthful route-specific contexts. System keeps its separate real Runtime/Backup/Security presentation.

Fresh pixels also isolate the last normal workspace route-context mismatch: Sources still shows the Chat-oriented `CHAT / NONE`, `No conversation selected` and Chat-specific inspector copy.

## Bounded product slice in this candidate

Only Sources is changed. The existing `NavigationContextAccessibility` presentation controller now maps row 4 to a truthful explicit no-selection state:

- `SOURCE / NONE`
- `No source selected`
- `DETAILS`: instructs the user to select a source before reported metadata/processing state can be shown.
- `PROVENANCE`: explicitly states provenance appears only for a selected reported source.

No source metadata, processing state, provenance, health, selection, Backend, Storage or Security fact is synthesized. The existing router remains `navigation.setCurrentRow(...)`; no parallel navigation path is introduced.

The focused Qt unit contract is extended to select Sources and require the exact route identity plus DETAILS/PROVENANCE/no-selection copy. Chat still hides the contextual overlay.

## Verification discipline

Before mutation, the current worker had no queued/in-progress run. Current Develop exact canonical Quality is green. The product/test/docs changes are assembled as one candidate commit so its subsequent visual evidence can refer to one immutable SHA.

At document creation time, fresh AFTER pixels for this candidate do not yet exist. No post-mutation MATCH or visual-improvement claim is made here. The next required action is exact-SHA Windows visual CI, followed by opening all eleven AFTER PNGs and specifically checking Sources BEFORE (`CHAT / NONE`) -> AFTER (`SOURCE / NONE`).

## Current slot state before this candidate's AFTER render

| Slot | Status |
|---|---|
| 01 ComfyUI | GAP — standalone versus integrated reference |
| 02 PALLAS | GAP — standalone/simplified versus rich shell + provenance reference |
| 03 Settings | GAP / STATE_UNVERIFIED — correct context, reconnecting versus healthy reference |
| 04 Help | GAP — standalone versus integrated Help workspace |
| 05 Workspace/Evidence | UNVERIFIED — empty/reconnecting versus populated synthesis/evidence state |
| 06 Jobs | GAP / STATE_UNVERIFIED — correct empty context versus running populated reference |
| 07 Command Palette | GAP / CONTEXT_UNVERIFIED — standalone dialog versus Knowledge overlay reference |
| 08 System | GAP / STATE_UNVERIFIED — real fail-closed state versus healthy reference |
| 09 Research | GAP / STATE_UNVERIFIED — correct route context but failed/empty state |
| 10 Light workspace | UNVERIFIED — no same-state real light rendering |
| 11 Local Memory/Knowledge | GAP / STATE_UNVERIFIED — correct route context but empty versus populated reference |

Strict accounting before the candidate AFTER render: `PAIRS_VERIFIED_0_OF_11 · MATCH_0_OF_11`.

## Readiness / next work

Worker remains materially divergent from current Develop; no Integrator-ready claim is made. Once exact-SHA AFTER evidence confirms the Sources correction without regression, the repeated generic Chat-inspector mismatch across normal non-Chat workspace routes can be treated as reduced. The next visual slice must then be chosen from fresh pixels, with shared shell/workspace hierarchy and standalone surface integration currently the strongest candidates.
