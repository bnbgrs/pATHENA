# Windows acceptance handoff — candidate `5875a2c`

- Candidate SHA: `5875a2cb15a3429db9121e1fd4e0033a6082212f`
- Harness SHA: `4712a0fd53da219532159c079f2b7792ab143fdb`
- Product verdict: **NOT_EXECUTABLE**
- Promotion recommendation: **DO NOT PROMOTE**

## Executed

- Exact remote candidate identity read and bound to this report.
- Existing safe-harness contract rechecked: workflow run `32799370991` passed its
  source-only validation on Ubuntu 24.04 / Python 3.12; the Windows Server 2025
  product job was correctly skipped.
- Lead handoff and candidate delta reviewed. Candidate `5875a2c` adds CORE-003
  Research Review but does not contain the pending PALLAS lifecycle or dialog-focus
  repairs.

No Windows product environment was instantiated for this SHA. Re-running a product
candidate with already reproduced targeted blockers cannot produce promotion GREEN
and would bypass the agreed preflight boundary.

## Not executed for this candidate

- Windows installation, import, desktop start, and full process-tree sampling
- database write, close, and reopen
- reachable and unreachable LM Studio loopback paths
- navigation and state checks for all eleven target areas
- Settings persistence and Jobs start/pause/cancel
- PALLAS start, interaction, pause/resume, and mini/full-screen state
- GUI screenshots and structured comparison with the eleven references
- package/EXE build and cold start

No Windows PASS, visual comparison, screenshot, EXE, ZIP, or promotionsfähiges
artifact exists for this SHA. Visual status is **NOT_EXECUTABLE**. RX 7900 XTX,
driver behavior, and real LM Studio inference remain `LOCAL_HARDWARE_PENDING`.

## Reproduced blockers and ownership

1. Two PALLAS lifecycle failures in
   `tests/unit/test_pathena_pallas_target_lifecycle.py`: CORE-002 and DS-002 are not
   yet integrated into the candidate. DS-002 is READY only on its isolated branch.
2. The transient Help-dialog focus restoration test
   `tests/unit/test_pathena_transient_dialog_shortcuts.py::test_transient_handoff_restores_original_workspace_focus_after_escape`
   still awaits DS-003.

The CORE-003-only candidate change does not affect these failure paths. The next
eligible Windows acceptance is a new exact candidate SHA after the Lead integrates
CORE-002 with DS-002 and DS-003, then reruns the three targeted blocker tests. That
new SHA must be dispatched once through the published harness. Any unbounded process
growth, 20 or more product/descendant processes, runner shutdown, orphan cleanup, or
remaining product process ten seconds after controlled exit is immediately RED.
