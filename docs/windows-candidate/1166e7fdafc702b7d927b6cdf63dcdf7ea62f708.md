# Windows acceptance handoff — candidate `1166e7f`

- Candidate SHA: `1166e7fdafc702b7d927b6cdf63dcdf7ea62f708`
- Harness SHA: `4712a0fd53da219532159c079f2b7792ab143fdb`
- Product verdict: **NOT_EXECUTABLE**
- Promotion recommendation: **DO NOT PROMOTE**

## Executed

- Source-only harness contract on Ubuntu 24.04 / Python 3.12: **PASS**
- Workflow run: `32799370991`
- Contract coverage: immutable candidate input, live-branch identity check, no dependency
  cache, full Windows process-tree sampling, recursion/growth threshold, ten-second
  orphan check, database reopen driver, LM Studio loopback probe, seven-page desktop
  navigation, non-promotable diagnostic artifacts only.

The Windows Server 2025 product job was deliberately **SKIPPED**. A branch push now
validates only the harness; candidate execution requires an explicit
`workflow_dispatch` SHA. This prevents a stale or known-blocked candidate from being
executed automatically.

## Not executed for this candidate

- Windows installation/import/desktop start
- database write/close/reopen
- provider reachable/unreachable loopback
- Settings persistence and Jobs transitions
- PALLAS interaction and pause/resume
- GUI screenshots and eleven-reference comparison
- package/EXE build and cold start

No Windows PASS, screenshots, EXE, ZIP, or promotionsfähiges Artefakt exists for this
SHA. Visual deviations are therefore **NOT_EXECUTABLE**, not PASS. RX 7900 XTX,
driver, and real LM Studio inference remain `LOCAL_HARDWARE_PENDING`.

## Blocking reason and next boundary

The candidate is already blocked by reproducible PALLAS owner-binding and transient
Help-focus failures in the Lead matrix. Running it now cannot produce promotion
GREEN. After the paired CORE-002/DS-002 repair and DS-003 are integrated, dispatch
the resulting exact new candidate SHA once through the published harness. Any
process growth, 20 or more relevant processes, cleanup requirement, runner shutdown,
or remaining product process after ten seconds is immediately RED.
