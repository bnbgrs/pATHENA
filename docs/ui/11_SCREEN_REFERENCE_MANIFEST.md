# pATHENA 11-Screen Reference Manifest

Current Develop inspected first: `develop/pathena-next@4dbefe2167b28bffab2c6b69b7a8df4b43770a6f`.
Current worker BEFORE candidate: `postmerge/ui@dce6d463b17474ec2da702a14b7a4365123df45d`.
Candidate in this commit is history-preservingly based on both heads and changes only the UI-owned PALLAS shell host, its focused tests, visual capture, and these UI evidence documents.

## Evidence rule used in this run

All eleven original user reference PNGs in `/pATHENA/Designreferenz – 11 Screenshots` were opened directly. All eleven real native Windows/PySide6 runtime PNGs from exact worker `dce6d463b17474ec2da702a14b7a4365123df45d` (`pathena-visual-dce6d463b17474ec2da702a14b7a4365123df45d`) were also opened directly. A technical runtime PNG is not counted as a verified pair when it does not represent the same reference state. `MATCH` remains forbidden without an opened same-state pair.

Strict BEFORE count: `PAIRS_VERIFIED_1_OF_11`, `MATCH_0_OF_11`.

| Slot | Reference | Current BEFORE render | Checked branch + SHA | Visible comparison | Status | Concrete next correction |
|---|---|---|---|---|---|---|
| 01 ComfyUI | YES — opened `ComfyUI-Integration im pATHENA Studio.png` | YES — opened `11-comfyui.png`, but detached utility state | `postmerge/ui@dce6d463…` | App shell/navigation: missing in current; workspace hierarchy: utility dialog only; inspector: missing reference Connection column; controls: real local controls exist but composition differs; typography/spacing/ratios: much denser/smaller; colors/contrast: same dark family but not reference geometry; borders/radius: utility-dialog treatment; state: diagnostic connected, not exact reference workflow. | `GAP` | After PALLAS, host real ComfyUI surface in shared shell with real connection context; do not invent workflow/model data. |
| 02 PALLAS | YES — opened `PALLAS – Lebendes semantisches Wissensfeld.png` | YES — opened `08-pallas.png`, real renderer but detached | `postmerge/ui@dce6d463…` | App shell/navigation: absent in BEFORE; workspace: graph exists but fills standalone window; inspector: shared Provenance/History context absent from capture; controls: real canvas only; typography/spacing/ratios: renderer is materially smaller and flatter; colors: compatible dark semantic palette; borders/radius: standalone frame; interactions: real node focus; state: deterministic real renderer fixture, not same Adaptive-memory reference state. | `GAP` | **Candidate in this commit:** host the real synchronized `PallasWorkspace` inside `referenceBody`, retain seven primary routes and shared inspector, and capture the whole main window. AFTER requires exact candidate render. |
| 03 Settings | YES — opened `pATHENA Einstellungen für lokale KI.png` | YES — opened `07-settings.png`; runtime provider unavailable | `postmerge/ui@dce6d463…` | App shell present; hierarchy is flatter; inspector has truthful unavailable state rather than healthy System status; controls are real; typography/spacing/ratios remain more utilitarian; colors/borders broadly aligned; state differs materially. | `GAP / STATE_UNVERIFIED` | Recompare when a truthful healthy local-provider state is available; do not synthesize health. |
| 04 Help | YES — opened `pATHENA Hilfe: Fähigkeiten im Überblick.png` | YES — opened `10-help.png`; corresponding Help state | `postmerge/ui@dce6d463…` | App shell and secondary navigation now present; workspace hierarchy/search/live capability rows exist; Help inspector is real; typography, icon treatment, row spacing/ratios and inspector compactness still differ; colors/contrast broadly aligned; interactions are real. | `GAP` | Defer further Help polish while larger shell gaps remain. This is the one strict same-state pair in the BEFORE set. |
| 05 Dark Workspace / Evidence | YES — opened `pATHENA im eleganten Dunkelmodus.png` | YES — opened `01-chat.png`, but reconnecting/empty | `postmerge/ui@dce6d463…` | Shell/rail/composer exist; reference populated Synthesis, graph/reasoning and Evidence/Activity are absent because current state is reconnecting; typography/spacing/ratios therefore cannot be same-state judged; current empty-state interactions are truthful. | `UNVERIFIED` | Obtain a real populated grounded-chat state; then compare hierarchy/graph/evidence without fabricated provenance. |
| 06 Jobs | YES — opened `pATHENA Jobs: Prüfung der Speicher-Richtlinie.png` | YES — opened `04-jobs.png`, empty job state | `postmerge/ui@dce6d463…` | Shell present; reference secondary job list, running stepper/live log and Execution inspector are not present in empty state; typography/spacing/ratios and state cannot be equivalent; controls remain truthful. | `GAP / STATE_UNVERIFIED` | Capture a real active durable job and only then align list/stepper/log composition. |
| 07 Command Palette | YES — opened `pATHENA Such- und Befehlspalette.png` | YES — opened `09-command-palette.png`, detached dialog | `postmerge/ui@dce6d463…` | Reference is an overlay over Knowledge workspace; current capture is isolated dialog; real commands/keyboard path exist; underlying navigation/workspace/inspector context is missing; geometry and ratios differ strongly. | `GAP / CONTEXT_UNVERIFIED` | After detached-surface work, render palette over the real workspace rather than as an isolated visual target. |
| 08 System | YES — opened `pATHENA Systemübersicht für lokale KI.png` | YES — opened `06-system.png`, truthful unavailable state | `postmerge/ui@dce6d463…` | Shell present; reference has healthy runtime/storage/connectivity/background-work and Security posture; current unavailable state is sparser; colors/contrast are aligned but hierarchy/spacing/content ratios differ because state differs. | `GAP / STATE_UNVERIFIED` | Recompare only with real healthy telemetry; no fabricated security/storage status. |
| 09 Research | YES — opened `pATHENA – Dunkles Studio für Wissensforschung.png` | YES — opened `03-research.png`, empty/failure state | `postmerge/ui@dce6d463…` | Shell present; reference has Synthesis, reasoning map, semantic graph and Evidence/Activity; current state is empty, so hierarchy/inspector/ratios are not comparable; controls are real. | `GAP / STATE_UNVERIFIED` | Capture a real populated research run before layout tuning. |
| 10 Light Workspace | YES — opened `pATHENA: Intelligenzstudio für lokales Wissen.png` | NO same-state Light runtime | `postmerge/ui@dce6d463…` | Reference proves light-theme geometry for shell, synthesis, graph, evidence and composer. Existing native harness has no corresponding light same-state target. | `UNVERIFIED / CURRENT_RENDER_UNAVAILABLE` | Add a real light-theme runtime target only when the product exposes that state; do not simulate it. |
| 11 Local Memory | YES — opened `pATHENA: Lokales Gedächtnis neu gedacht.png` | YES — opened `02-knowledge.png`, empty Knowledge state | `postmerge/ui@dce6d463…` | Shell present; reference is populated Synthesis + reasoning graph + Evidence/Activity; current Knowledge state is empty; typography/spacing/ratios and inspector cannot be same-state judged. | `GAP / STATE_UNVERIFIED` | Capture real populated durable-memory data, then tune composition around actual provenance. |

## Current candidate: PALLAS shell integration

BEFORE, `PallasFullViewController` explicitly created a separate `QDialog`, and the visual harness captured that dialog alone. This produced the largest recurring shell break visible in the opened references. The candidate in this commit keeps the real `PallasWorkspace` and shared semantic controller but parents the full workspace into the existing `referenceBody` between the icon rail and shared inspector. Opening PALLAS hides only the routed center workspace; choosing any of the existing seven primary routes restores it. No eighth route, synthetic semantic object, Backend/Storage/Security behavior, or guard change is introduced.

The native visual harness is changed only so Screen 02 captures the real main window while shell-hosted PALLAS is visible and fails if PALLAS escapes into a detached dialog. Exact AFTER visual status remains `UNVERIFIED` until that candidate artifact is produced and opened.

`VISUAL_READY_11_OF_11=NO`
