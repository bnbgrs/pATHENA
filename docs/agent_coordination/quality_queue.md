# pATHENA Quality Queue

Persistent queue for quality verification on `agent/pathena`.

Status values: `READY`, `IN_PROGRESS`, `BLOCKED`, `DONE`, `STALE`.

| ID | Priority | Slice | Evidence | Ownership | Status | Last verification |
| --- | --- | --- | --- | --- | --- | --- |
| QG-2097-MYPY-RESEARCH-MODELS | P0 | Resolve/verify two mypy errors in `src/athena/research/models.py` | CI run #2097: operator error after `_nonnegative_int()` plus assignment inference conflict; current helper still returns `None`, so callers cannot rely on it for static narrowing of `object` | BACKEND | BLOCKED | 2026-08-23, HEAD `025ca1ab6657e08748cbe96c148f021c97b0c3d2`; narrowing helper pattern re-read and still present |
| QG-2097-MYPY-RESEARCH-IDEMPOTENCY | P0 | Resolve/verify three mypy unreachable errors in `src/athena/research/idempotency.py` | CI run #2097: typed `Sequence[tuple[...]]` is combined with runtime guards whose branches are statically unreachable under the declared type | BACKEND | BLOCKED | 2026-08-23, HEAD `025ca1ab6657e08748cbe96c148f021c97b0c3d2`; typed Sequence plus tuple/sequence runtime validation re-read, no executed fix verification |
| QG-2097-MYPY-SEMANTIC | P0 | Resolve/verify two mypy errors in `_persisted_int()` | CI run #2097: `int(object)` overload and resulting `Any` return; current implementation still calls `int(value)` while `value` is typed `object` | BACKEND | BLOCKED | 2026-08-23, HEAD `025ca1ab6657e08748cbe96c148f021c97b0c3d2`; problematic conversion re-read and still present |
| QG-MIGRATION-JOURNAL-ANCESTOR-BOUNDARY | P1 | Reject symlink/junction/reparse-point ancestors before migration-journal reads or temporary writes | Current `MigrationJournalStore.load()` checks only the journal file itself before following ancestors; `publish()` creates/fsyncs its temporary file before `durable_replace()` performs the stronger ancestor check. Error log: `docs/quality-gate/2026-08-23-migration-journal-symlink-ancestor-boundary.md`. | BACKEND | BLOCKED | 2026-08-23, HEAD `1d5ce904c80a1122ec3204499326363b1050b767`, journal blob `8cdcdceb443d3206b8982aebe39ba48c242d38dd`; static primary defect confirmed, no runtime PASS claimed |
| QG-MIGRATION-CLONE-REPARSE-BOUNDARY | P1 | Reject Windows junction/reparse-point ancestors before migration clone source/destination traversal | `migration_clone._reject_symlink_path()` checks only `Path.is_symlink()` although the repository's hardened `durable_fs` boundary also rejects junctions and Windows reparse points. Error log: `docs/quality-gate/2026-08-23-migration-clone-windows-reparse-boundary.md`. | BACKEND | BLOCKED | 2026-08-23, HEAD `64c78c110415b464b7f792a526ee1aed2fcac849`, clone blob `d7006f9c4a87ed2cba8c3d77ab1ded0a860b323e`; static Windows trust-boundary defect confirmed, native execution pending |
| QG-CI-SUPERSEDED-RUNS | P1 | Keep CI useful under continuous branch writes without accumulating stale runs or repeatedly killing active evidence | Initial `cancel-in-progress: true` cancelled active gates under fast churn. Final policy keeps `cancel-in-progress: false`, one PR gate per feature-branch commit and `push` full gate only on `main`. GitHub concurrency still retains only one pending run per group, so older pending runs can be replaced while an active run is preserved. | QUALITY/GATE | IN_PROGRESS | 2026-08-23; runs #2572, #2580, #2591, #2594 and #2595 observed cancelled while pending/superseded; active-run survival/completion remains the required executed evidence |
| QG-CI-FAILFAST-COVERAGE | P1 | Ensure CI exposes downstream pytest regressions even when an earlier check is red, without changing local default fail-fast behavior | `scripts/quality.py --keep-going` is implemented and `.github/workflows/quality.yml` invokes it; conclusive completed-run log still required | QUALITY/GATE | IN_PROGRESS | 2026-08-23; no final-policy run has completed yet, so downstream pytest evidence remains pending |
| QG-QUALITY-HARNESS-REGRESSION | P1 | Regression-cover default fail-fast, keep-going, stable branch-churn scheduling, local-smoke, and targeted Windows path-safety coverage | `tests/unit/test_quality_script.py` covers default short-circuit and keep-going execution; `tests/unit/test_quality_workflow_contract.py` covers concurrency policy, main-only push full gate, PR coverage, `--keep-going`, disposable local smoke and targeted Windows path safety | QUALITY/GATE | IN_PROGRESS | 2026-08-23; Windows contract commit `4e608f5d1890605985ce56060cf2ff9c1c2f6f9d`; local-smoke contract commit `daa8609ac27b0f15d78bb3c801451f2e8dc1bfeb`; execution pending |
| QG-FG-002-REGRESSION | P1 | Verify newly implemented ModelRegistry / active-primary-model layer without changing Backend-owned code | `src/athena/model/registry.py` and `tests/unit/test_model_registry.py` were statically audited: canonical provider/model IDs, duplicate/unknown rejection, active-model validation and disappearance handling have targeted coverage | QUALITY/READ-ONLY | IN_PROGRESS | 2026-08-23; static contract/test audit completed, execution evidence pending |
| QG-FG-004-REGRESSION | P1 | Verify provider capability normalization and unknown-vs-unsupported behavior | `src/athena/model/domain.py` and `tests/unit/test_model_capabilities.py` statically preserve SUPPORTED/UNSUPPORTED/UNKNOWN semantics; absent/unrecognized metadata remains UNKNOWN rather than invented unsupported | QUALITY/READ-ONLY | IN_PROGRESS | 2026-08-23; static contract/test audit completed, execution evidence pending |
| QG-FG-005-REGRESSION | P1 | Verify Context Builder source-diversity behavior, rank-1 preservation, duplicate deferral and contradiction exemption | Implementation in `src/athena/retrieval/context.py` preserves rank-1 and defers duplicate non-contradictions; `tests/unit/test_context_builder_diversity.py` explicitly covers diverse-source preference and contradiction-bearing claim protection | QUALITY/READ-ONLY | IN_PROGRESS | 2026-08-23; targeted test file found and statically audited, execution evidence pending |
| QG-FG-007-REGRESSION | P1 | Verify model-load ownership prevents automatic unload of external/unknown loads | `tests/unit/test_model_load_ownership.py` statically covers fail-closed unknown/external ownership, ATHENA-owned auto-unload permission, and refresh clearing ownership even while `loaded=True`; no executed PASS is available yet | QUALITY/READ-ONLY | IN_PROGRESS | 2026-08-23, static test audit completed; runtime verification pending |
| QG-FG-012-EXECUTION-GUARD | P1 | Verify the new protected-runtime execution guard remains fail-closed and persistence-safe while FG-012 is wired end-to-end | Current guard verifies bundle construction and immediate pre-provider recheck; durable metadata excludes plaintext/rendered context/document/quote hashes. New boundary tests cover empty/malformed values, bool/nonnegative counters, budget overflow, relock propagation, mutation/reverification and metadata safety | QUALITY/READ-ONLY | IN_PROGRESS | 2026-08-23; latest protected-execution implementation/tests statically audited, runtime execution pending |
| QG-FG-014-REGRESSION | P1 | Verify active SQLite state-root locality enforcement across native Windows probing, deterministic remote-path classification and pre-SQLite refusal | `src/athena/storage/locality.py`, `RuntimeLayoutService.start()`, and `inspect_database_read_only()` were audited. Existing tests cover UNC, mapped drives, unverifiable Windows drive types, NFS/CIFS, escaped mountinfo and longest-mount selection; preflight test proves SQLite is not opened after locality rejection. A dedicated Windows CI lane now also probes the real `GetDriveTypeW` path. | QUALITY/READ-ONLY + GATE | IN_PROGRESS | 2026-08-23; static audit completed; Windows lane commits `a5462ca684ab1e202658cda33efdcbb0c2edf917` and `4e608f5d1890605985ce56060cf2ff9c1c2f6f9d`; executed PASS pending |
| QG-FG-013-MIGRATION-SAFETY | P1 | Regression-watch newly introduced migration safety contracts, journal durability and clone-first primitives without modifying Backend-owned implementation | `migration_safety.py` integer-only free-space arithmetic and boundary tests were statically audited. Follow-up journal and clone audits found two independent trust-boundary defects tracked by the dedicated migration-journal and migration-clone queue IDs. | QUALITY/READ-ONLY | IN_PROGRESS | 2026-08-23; free-space contract audit clean; migration journal and clone audits each produced a P1 Backend handoff; execution unavailable locally |
| QG-RECENT-REGRESSION-SCAN | P1 | Risk-based scan of recent commits for newly introduced lint/type/test regressions, prioritizing files changed after last verified baseline | Recent audits include protected execution, direct-chat controls, model registry/capabilities, signature guard, time range, schema IDs, provider-boundary compatibility, FG-014 locality and FG-013 migration safety/journal/clone work. | QUALITY/READ-ONLY | IN_PROGRESS | 2026-08-23; two confirmed Backend-owned migration trust-boundary defects logged; no foreign product code mutated |
| QG-WINDOWS-GATE-PARITY | P2 | Add focused Windows execution where platform behavior materially differs, without duplicating the complete Linux desktop gate | Quality workflow now has a 5-minute `windows-latest` path-safety job that executes the native active-state locality probe and focused storage-path regressions. UI Snapshot remains Linux-only; broader Windows coverage still needs evidence-driven expansion rather than a speculative full matrix. | QUALITY/GATE | IN_PROGRESS | 2026-08-23; targeted lane implemented at `a5462ca684ab1e202658cda33efdcbb0c2edf917`, contract test at `4e608f5d1890605985ce56060cf2ff9c1c2f6f9d`; execution pending |
| QG-PACKAGING-START-PATH | P2 | Execute the published `athena-local-smoke` entry point as a disposable install/start/restart/persistence check | `run_local_smoke()` performs real Core/API start-stop, durable chat recovery, schema and bootstrap-cleanup checks. A separate 5-minute Linux CI job now runs `athena-local-smoke --restart-cycles 1` so packaging/start verification does not consume the 10-minute full-gate budget. | QUALITY/GATE | IN_PROGRESS | 2026-08-23; workflow commit `d5a2ce47e23bebbc7aa248806f111089801403a3`, contract test `daa8609ac27b0f15d78bb3c801451f2e8dc1bfeb`; execution pending |
| QG-CI-TIMEOUT-HEADROOM | P2 | Verify that keep-going full gate fits the current 10-minute workflow timeout; adjust only on observed timeout evidence | Current Linux quality job retains `timeout-minutes: 10`; local-smoke and Windows path-safety each use separate 5-minute caps. Queued/cancelled runs have not produced timeout evidence supporting a Linux timeout change. | QUALITY/GATE | READY | 2026-08-23; no speculative Linux timeout increase |
| QG-FG-003-REGRESSION | P2 | Verify all normative provider health states remain represented and stable | Current `ProviderHealthStatus` exposes UNAVAILABLE/STARTING/READY/BUSY/DEGRADED/ERROR and boundary tests reject untyped status values | QUALITY/READ-ONLY | IN_PROGRESS | 2026-08-23; static domain/test audit completed, execution evidence pending |

## Current primary failure

The latest fully classified CI failure baseline remains historical run #2097:

- Specification validator: PASS (63/63)
- Ruff: PASS
- mypy: FAIL (7 errors / 3 Backend-owned files)
- pytest: NOT REACHED in that historical fail-fast run

A newer completed keep-going gate is still required to supersede that execution baseline. Independently of CI, current-code regression analysis has confirmed two newer Backend-owned P1 primary defects:

- `QG-MIGRATION-JOURNAL-ANCESTOR-BOUNDARY` — migration journal can traverse unsafe symlink/junction/reparse ancestors before the hardened publication boundary runs.
- `QG-MIGRATION-CLONE-REPARSE-BOUNDARY` — migration clone has a symlink-only ancestor guard and does not match the repository's Windows reparse/junction trust boundary.

Both have durable error logs under `docs/quality-gate/` and remain BLOCKED on Backend ownership.

The quality harness uses `--keep-going`, so the next completed Linux gate log must establish the new Specification/Ruff/mypy/pytest baseline and either close or refine `QG-CI-FAILFAST-COVERAGE`. GitHub concurrency with `cancel-in-progress: false` preserves an active run but does not preserve an unlimited pending backlog: at most one pending run exists per concurrency group, so pending supersession remains expected under very fast branch churn.

## Coverage findings awaiting execution or design

- Windows: a focused `windows-latest` path-safety lane now exercises the native active-state locality probe and existing storage locality/preflight/runtime tests. Execution evidence is still pending; broader Windows desktop/UI parity remains a separate evidence-driven question.
- Packaging/start path: a disposable `athena-local-smoke --restart-cycles 1` job is now part of Quality CI; execution evidence is pending.
- Protected execution: the FG-012 guard is statically fail-closed at its boundary, but its targeted tests and eventual orchestration path require executed verification before Quality can mark the slice done.
- FG-014: deterministic tests are strong at the classifier/preflight boundary; promotion from IMPLEMENTED to VERIFIED still requires executed CI evidence, especially the native Windows lane.
- FG-013 migration safety: pure free-space contracts are statically coherent, but migration journal and clone path trust boundaries need Backend hardening before Quality can treat clone-first migration storage as fail-closed on redirected paths.
