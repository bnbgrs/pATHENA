# pATHENA Security Queue

Persistent security queue for `agent/pathena`.

Status values: `FOUND`, `READY`, `IN_PROGRESS`, `BLOCKED`, `FIXED`, `VERIFIED`, `STALE`.

| ID | Priority | Category | Threat / attack surface | Evidence / reproduction | Assets / impact | Ownership | Status | Fix / mitigation | Last verification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SEC-001 | P1 | Local API / secrets / Windows ACL | The bearer token for the loopback Core API is persisted to `core-api.token`; `_write_private_text()` requests mode `0o600`, but Windows does not provide POSIX permission-bit isolation equivalent to a private DACL. A second local account/process with filesystem access to the runtime root could reuse the bearer token and obtain the authenticated local API surface. | `src/athena/api/runtime.py`: `LocalApiRuntime.publish()` writes a random bearer token through `_write_private_text(..., mode=0o600)`. Static review only; no real Windows ACL exploit claimed. | Local chat/knowledge/jobs/system API and shutdown capability when enabled. | SECURITY/RELEASE | READY | Define and regression-test a Windows runtime-root invariant: token/discovery state must live under a per-user directory whose DACL excludes other interactive users; if current runtime-root construction cannot guarantee this, add Windows ACL hardening using established Windows APIs/libraries. Preserve symlink/reparse-point protections. | 2026-08-23, static code-path review |
| SEC-002 | P2 | External transport policy | `ExternalFetchGateway` supports both `http://` and `https://`; an explicitly authorized direct request may therefore traverse plaintext HTTP. This is not an authorization bypass and may reflect product intent. | `src/athena/external/gateway.py` permits HTTP(S); existing approval binding, SSRF controls, DNS-rebinding resistance and response limits remain separate controls. | Confidentiality/integrity for explicitly approved clearnet HTTP destinations. | SECURITY/FEATURE | FOUND | Verify product/spec intent before mutation. If compatible, prefer HTTPS-only by default with explicit high-friction opt-in and provenance marking for plaintext HTTP. | 2026-08-23, static gateway review |
| SEC-003 | P1 | Local model provider / proxy bypass | Historical adapter code used ambient/default urllib transport for loopback LM Studio calls. Current provider code routes all reviewed discovery/chat/structured requests through the shared local-only transport instead. | Fresh current-remote trace: `src/athena/model/adapters/lm_studio.py` imports `Request` but no direct `urlopen`; discovery, streamed chat, structured generation and controlled structured generation use `open_local_request()`. `src/athena/model/adapters/local_http.py` revalidates loopback HTTP(S), installs `ProxyHandler({})`, rejects redirects and validates finite positive timeouts. | Potential prompt/model-metadata disclosure to an ambient proxy and violation of the local-only trust boundary. | BACKEND | FIXED | Preserve the shared proxy-disabled loopback transport for all LM Studio calls. Promote to VERIFIED only after observed hostile-proxy targeted/CI execution; no test pass is claimed from this static revalidation. | 2026-08-24, fresh current-remote static trace under CI state UNKNOWN |
| SEC-004 | P2 | Protected Content / KDF resource bounds | Persisted Argon2id parameters previously lacked upper bounds, allowing corrupted metadata to request pathological CPU/RAM before authenticated key recovery. | `src/athena/security/models.py` now caps v1 at 10 iterations, 16 lanes and 256 MiB. `tests/unit/test_security_argon2_parameter_validation.py` covers ceilings/pathological metadata without running pathological KDF allocations. Fix commits `be5a7f06d2f71f011aae7f30a02671ff9a5ebd18` and `fd700b85dcf8e4cbe7bc6289e7af31203c2fd0b9`. | Availability during unlock/recovery. | SECURITY | FIXED | Keep defaults unchanged; promote to VERIFIED only after targeted/CI execution is actually green. | 2026-08-23, static path verification; no green execution claimed |
| SEC-005 | P2 | CI supply chain | Workflows execute external Actions via mutable major-version tags rather than immutable reviewed commit SHAs. | Current `.github/workflows/quality.yml` still uses `actions/checkout@v6` and `actions/setup-python@v6`; Python packages themselves are exact-pinned/locked. | CI runner integrity, artifacts and any future workflow secrets. | QUALITY | READY | Pin external Actions to reviewed full commit SHAs, retaining release-tag comments for maintainability. | 2026-08-23, current workflow re-check |
| SEC-006 | P1 | Filesystem / Raw Archive confinement | Blob publication remains vulnerable to a path-replacement race because `_copy_into_root()` opens its temporary blob by pathname after parent validation. The shared `durable_replace()` now binds POSIX rename to opened parent FDs, but that occurs only after the temporary content has already been created/written. | `src/athena/source/blob_store.py` still creates a temp blob after `durable_mkdir()` using pathname-based file creation. Current `durable_fs.py` improves POSIX publication identity, but cannot retroactively protect bytes written before publication. | Integrity/confinement of Durable Spool and Archive Root; possible creation of attacker-directed blob temp content outside the root when the attacker already has mutation access to a storage-tree ancestor. | BACKEND/SECURITY | READY | Move blob temporary creation/write onto an identity-bound primitive before any bytes are written. Preserve current POSIX dirfd semantics and add equivalent Windows handle/reparse-safe behavior plus deterministic race coverage. | 2026-08-23, residual re-check after durable-fs parent-identity work |
| SEC-007 | P1 | Filesystem / destructive operations TOCTOU | Raw Archive deletion/cleanup paths also rely on pathname checks followed by later pathname-based hash/unlink operations. An external actor that can replace a previously validated parent directory with a link/junction between verification and `unlink()` can redirect deletion to a same-named file outside the configured storage root. The application mutation lock serializes ATHENA writers but does not exclude out-of-band filesystem mutation. | `cleanup_verified_spool_replica()`, `purge_verified_replicas()` and orphan reconciliation still require object/parent identity to remain bound through destructive use; current durable-write/replace improvements do not provide a shared identity-bound unlink primitive. | Availability/integrity of files reachable through a hostile parent redirection, subject to the attacker's pre-existing ability to mutate a Raw Archive ancestor. | BACKEND/SECURITY | READY | Add an identity-safe confined deletion primitive and use it for cleanup/purge/orphan deletion. Add deterministic parent-replacement race tests and Windows junction/reparse coverage. | 2026-08-23, static destructive-path re-check |
| SEC-008 | P2 | Backup target / resource exhaustion | Backup deletion-ledger reads previously lacked explicit per-file, record-count, and aggregate-byte limits. | Current `src/athena/backup/deletion_storage.py` caps head and individual records at 64 KiB, record count at 250,000 and aggregate record bytes at 128 MiB; `_read_bounded_regular_file()` validates regular-file handle/path identity and size before bounded read. `tests/unit/test_backup_deletion_storage_resource_bounds.py` exists. | Availability of backup verification/synchronization and memory/CPU consumption when a backup target is corrupt, removable, shared or attacker-modifiable. | BACKEND/SECURITY | FIXED | Keep the current bounded-handle reads and versioned limits. Promote to VERIFIED only after observed green targeted/CI execution. | 2026-08-23, static post-fix verification on current remote |
| SEC-009 | P1 | Storage migration / filesystem TOCTOU | Clone-first migration has gained identity-bound durable journal writes and POSIX durable replace, but the full migration still crosses check/use boundaries in clone creation, cleanup, Windows publication and activation/lock lifetime. | `migration_journal.py` now uses `durable_write_bytes`; `durable_replace()` uses opened parent FDs on POSIX. `sqlite3.connect(candidate)` and several cleanup/Windows operations remain pathname-addressed. | Integrity/confidentiality of the live database, rollback database, migration candidate and recovery journal. | BACKEND/SECURITY | READY | Complete BE-028 with identity-bound creation/cleanup/activation across all relevant OSes. Preserve POSIX dirfd improvements and add Windows handle/reparse-safe equivalents plus deterministic parent-replacement tests. | 2026-08-23, partial mitigation revalidated |
| SEC-010 | P2 | Storage migration / resource exhaustion | The external migration journal previously allowed an unbounded full-file read before JSON parsing. | Current `migration_journal.py` defines `_MAX_MIGRATION_JOURNAL_BYTES = 64 * 1024`; `_read_journal_file()` validates opened-handle identity, regular-file type and `st_size`, then reads at most max+1 and re-validates payload length. `tests/unit/test_migration_journal_resource_bounds.py` exists. | Availability of startup, migration recovery and administrative repair on a corrupted/hostile runtime filesystem. | BACKEND/SECURITY | FIXED | Preserve the 64-KiB pre-read/actual-read bound and handle identity checks. Promote to VERIFIED only after observed green targeted/CI execution. | 2026-08-23, static post-fix verification on current remote |
| SEC-011 | P1 | Local API / Windows reparse confinement | `LocalApiRuntime` previously used only `Path.is_symlink()` for runtime-root and ancestor validation, unlike the shared storage trust-boundary predicate that also recognizes Windows junctions and reparse points. | `src/athena/api/runtime.py` uses `durable_fs.is_link_boundary()` for runtime-root, ancestor and file-parent checks. `tests/unit/test_api_runtime_boundaries.py` contains deterministic reparse-boundary coverage. Fix commits `124fe5d0d5fc96a37c8bacb14879fb55908614b0` and `f1f7e60d378ba1bf677cd3a43e4472c5948cffc7`. | Confidentiality of the Core API bearer token and integrity of local discovery state on Windows. | SECURITY | FIXED | Keep shared junction/reparse predicate; obtain real targeted/Windows execution before VERIFIED. Concurrent parent replacement remains under SEC-012. | 2026-08-23, static post-fix review; tests present but current run pending |
| SEC-012 | P1 | Local API / token-publication TOCTOU | The API runtime previously created token/discovery staging files directly by pathname after validating the parent. It now delegates private publication to `durable_write_bytes()`. On POSIX that primitive binds create/write/publish to an opened parent FD and verifies directory identity, closing the secret-write race there. The Windows branch of `durable_write_bytes()` still creates the temporary file by pathname after a static parent check, so the Windows race remains. | Security commits `39c7b1aee5407b7537ea808070631e53ae2f5e67` and `ba05a63a7cf1002d111d2f2f28ecc90310e600cc`: `src/athena/api/runtime.py::_write_private_text()` now calls `durable_write_bytes(..., mode=0o600)` and API boundary regression fails closed when that writer rejects parent identity. `durable_fs.py` uses dirfd-relative open/replace on POSIX but pathname temp creation on Windows. | Confidentiality of the ephemeral bearer token; local API trust boundary, now mitigated on POSIX but not yet on Windows. | SECURITY | READY | Preserve shared `durable_write_bytes()` usage. Extend its Windows branch with established directory-handle/reparse-safe semantics so no token byte is written until parent identity is pinned. Add real/deterministic Windows parent-replacement coverage. | 2026-08-23, POSIX design closed statically; Windows residual remains; CI run 32666706129 pending |
| SEC-013 | P1 | Storage migration / cross-process lock lifetime | The migration lock verifies lock-file identity at acquisition but must preserve the same migration-root identity throughout the critical section. | Current migration lock has additional identity checks, but the security invariant remains that a root rename/replacement must not permit a second logically equivalent lock owner. | Exclusivity of schema migration and recovery assumptions. | BACKEND/SECURITY | READY | Complete BE-028 with stable root/directory identity for the entire migration critical section and a deterministic two-owner rename/replacement test. | 2026-08-23, static lock-lifetime risk retained pending full proof |
| SEC-014 | P1 | Database startup / preflight-to-writer TOCTOU | Startup validates the database and its ancestors before storage bootstrap, but the validated file/directory identity is not carried into the later live SQLite writer open. A local actor able to replace a validated ancestor between preflight and writer startup could make the preflight inspect one object while the live application opens another path target. The new reparse-point checks close static link boundaries, not this check/use race. | `AthenaApplication.start()` calls `inspect_database_read_only(self.paths.database_path)` and then `self.services.start_all()`; the current preflight checks `is_link_boundary()` and later resolves/opens SQLite by pathname. `StorageBootstrapService`/`SQLiteDatabase` subsequently open the configured database path independently. No active exploit was run; finding is from static cross-layer trace on HEAD `e1ea8489902c971426a6858c506dffab726ba4b7`. | Integrity of the canonical live SQLite database and the fail-closed guarantee that the database validated before startup is the same database later opened writable. | BACKEND/SECURITY | READY | Bind database/root identity across preflight and live writer activation, or re-establish identity atomically at the writer boundary and fail closed on mismatch. Add deterministic synchronization-barrier regressions for parent rename/replacement on POSIX and native Junction/Reparse replacement on Windows. Preserve the current static link/reparse checks. | 2026-08-24, static startup trace on exact remote HEAD |

## Verified baseline controls

These controls should not be re-audited without relevant code/dependency/configuration movement:

- `ExternalFetchGateway` direct mode rejects loopback/private/link-local/reserved targets and binds validated DNS results into the connection path; redirects are bounded and re-authorized.
- Tor mode delegates DNS to SOCKS, uses per-request stream-isolation credentials, constrains the proxy endpoint and fails closed rather than silently falling back to direct networking.
- Direct/Tor HTTPS paths use normal certificate and hostname validation; no permissive TLS context was found in the reviewed gateway path.
- Core API is IPv4-loopback-only, bearer-authenticated with constant-time comparison, rejects browser `Origin` requests and bounds request bodies.
- LM Studio configuration rejects non-loopback endpoint configuration; the current adapter transport is explicitly proxy-free and redirect-blocking through `open_local_request()`; SEC-003 remains FIXED pending observed test verification.
- Protected Content uses pyca/cryptography AES-256-GCM and Argon2id, random keys/nonces/salts and authenticated decryption; SEC-004 tracks KDF work-factor bounds.
- Native DOCX reads constrained OOXML parts without extracting archive members to filesystem paths. HTML parsing is bounded and non-executing. PDF parsing is isolated in a supervised child process with input/page/output/time/memory limits.
- Backup restore validates destination isolation, manifest/object paths, hashes and SQLite integrity before publication.
- Python/build dependencies are exact-pinned and locked with artifact hashes; SEC-005 tracks mutable GitHub Action references separately.

## Handoffs

- `SEC-001` -> RELEASE/SECURITY: prove or establish a private Windows DACL invariant for runtime token/discovery state.
- `SEC-002` -> FEATURE: decide whether plaintext HTTP remains part of the intended external-fetch contract.
- `SEC-003` -> QUALITY/SECURITY: implementation is present; execute hostile-proxy/redirect/loopback regressions and promote to VERIFIED only with observed green evidence.
- `SEC-004` -> SECURITY/QUALITY: promote only after observed green targeted/CI execution.
- `SEC-005` -> QUALITY: pin executable external Actions immutably.
- `SEC-006` -> BACKEND: use identity-bound blob temp creation/write before publication; current durable replace alone is too late.
- `SEC-007` -> BACKEND: add identity-safe confined deletion for cleanup/purge/orphan paths.
- `SEC-008` -> QUALITY: execute the new deletion-ledger resource-bound regressions and promote only on observed green evidence.
- `SEC-009` -> BACKEND/BE-028: complete identity-bound migration creation/cleanup/activation, especially Windows and sqlite candidate creation.
- `SEC-010` -> QUALITY: execute migration-journal resource-bound regressions and promote only on observed green evidence.
- `SEC-011` -> QUALITY: execute targeted API-runtime boundary tests on Linux and Windows; promote only with observed green evidence.
- `SEC-012` -> SECURITY: POSIX secret publication is now identity-bound through `durable_write_bytes`; close the remaining Windows parent-replacement window in that shared primitive.
- `SEC-013` -> BACKEND/BE-028: migration lock ownership must survive migration-root rename/replacement for the full critical section.
- `SEC-014` -> BACKEND: preserve the validated database/root identity through live writer startup, or revalidate identity at the writer boundary atomically; add deterministic cross-platform parent-replacement regressions.
