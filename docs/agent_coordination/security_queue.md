# pATHENA Security Queue

Persistent security queue for `agent/pathena`.

Status values: `FOUND`, `READY`, `IN_PROGRESS`, `BLOCKED`, `FIXED`, `VERIFIED`, `STALE`.

| ID | Priority | Category | Threat / attack surface | Evidence / reproduction | Assets / impact | Ownership | Status | Fix / mitigation | Last verification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SEC-001 | P1 | Local API / secrets / Windows ACL | The bearer token for the loopback Core API is persisted to `core-api.token`; `_write_private_text()` requests mode `0o600`, but Windows does not provide POSIX permission-bit isolation equivalent to a private DACL. A second local account/process with filesystem access to the runtime root could reuse the bearer token and obtain the authenticated local API surface. | `src/athena/api/runtime.py`: `LocalApiRuntime.publish()` writes a random bearer token; `_write_private_text()` creates/chmods the file with `_PRIVATE_MODE = 0o600`. Static review only; no real Windows ACL exploit claimed. | Local chat/knowledge/jobs/system API and shutdown capability when enabled. | SECURITY/RELEASE | READY | Define and regression-test a Windows runtime-root invariant: token/discovery state must live under a per-user directory whose DACL excludes other interactive users; if current runtime-root construction cannot guarantee this, add Windows ACL hardening using established Windows APIs/libraries. Preserve symlink/reparse-point protections. | 2026-08-23, static code-path review |
| SEC-002 | P2 | External transport policy | `ExternalFetchGateway` supports both `http://` and `https://`; an explicitly authorized direct request may therefore traverse plaintext HTTP. This is not an authorization bypass and may reflect product intent. | `src/athena/external/gateway.py` permits HTTP(S); existing approval binding, SSRF controls, DNS-rebinding resistance and response limits remain separate controls. | Confidentiality/integrity for explicitly approved clearnet HTTP destinations. | SECURITY/FEATURE | FOUND | Verify product/spec intent before mutation. If compatible, prefer HTTPS-only by default with explicit high-friction opt-in and provenance marking for plaintext HTTP. | 2026-08-23, static gateway review |
| SEC-003 | P1 | Local model provider / proxy bypass | `LMStudioProvider` sends local prompts/model requests through plain `urllib.request.urlopen()`. The adapter models LM Studio as loopback-only, but the default urllib opener can honor ambient process/OS HTTP(S) proxy configuration. | Current `src/athena/model/adapters/lm_studio.py` imports `urlopen` and uses it directly for streamed generation; configuration separately restricts LM Studio URLs to loopback. No external traffic generated during this audit. | Potential prompt/model-metadata disclosure to an ambient proxy and violation of the local-only trust boundary. | BACKEND | READY | Replace ambient/default proxy handling with an explicit proxy-disabled opener/transport. Add a hostile `HTTP_PROXY`/`HTTPS_PROXY` regression proving loopback requests bypass ambient proxies. Preserve loopback URL validation and timeouts. | 2026-08-23, re-traced on current remote; still present |
| SEC-004 | P2 | Protected Content / KDF resource bounds | Persisted Argon2id parameters previously lacked upper bounds, allowing corrupted metadata to request pathological CPU/RAM before authenticated key recovery. | `src/athena/security/models.py` now caps v1 at 10 iterations, 16 lanes and 256 MiB. `tests/unit/test_security_argon2_parameter_validation.py` covers ceilings/pathological metadata without running pathological KDF allocations. Fix commits `be5a7f06d2f71f011aae7f30a02671ff9a5ebd18` and `fd700b85dcf8e4cbe7bc6289e7af31203c2fd0b9`. | Availability during unlock/recovery. | SECURITY | FIXED | Keep defaults unchanged; promote to VERIFIED only after targeted/CI execution is actually green. | 2026-08-23, static path verification; no green execution claimed |
| SEC-005 | P2 | CI supply chain | Workflows execute external Actions via mutable major-version tags rather than immutable reviewed commit SHAs. | `.github/workflows/quality.yml` / UI workflow previously showed `actions/checkout@v6`, `actions/setup-python@v6`, `actions/upload-artifact@v4`; Python packages themselves are exact-pinned/locked. | CI runner integrity, artifacts and any future workflow secrets. | QUALITY | READY | Pin external Actions to reviewed full commit SHAs, retaining release-tag comments for maintainability. | 2026-08-23, static workflow review |
| SEC-006 | P1 | Filesystem / Raw Archive confinement | Blob publication is still vulnerable to a path-replacement race even after static symlink/junction checks were added. `durable_mkdir()` validates link/reparse ancestors, but `_copy_into_root()` subsequently opens `temp_path` by pathname with `"xb"`; an external local actor able to mutate the storage tree can replace a validated parent directory with a symlink/junction between validation and open. `durable_replace()` re-checks later, but by then the temporary blob may already have been created and written outside the configured root. | Current `src/athena/source/blob_store.py` calls `durable_mkdir(final_path.parent, ...)`, then `temp_path.open("xb")`, hashes the temp file, and later calls `durable_replace()`. Current `src/athena/storage/durable_fs.py` performs path-based `_assert_real_directory()` / `_is_link_boundary()` checks before operations, but holds no directory handle/identity across the later pathname open. Existing tests cover static symlink/reparse boundaries, not replacement between check and use. No exploit was executed. | Integrity/confinement of Durable Spool and Archive Root; possible creation of attacker-directed blob temp content outside the root when the attacker already has mutation access to a storage-tree ancestor. | BACKEND/SECURITY | READY | Treat BE-011's static link/reparse hardening as partial, not sufficient for hostile concurrent mutation. Required invariant: bind creation/publication to verified directory identities rather than only pre-checking pathnames. On POSIX prefer dirfd/openat-style no-follow/identity checks where feasible; on Windows use established handle/reparse-safe APIs. At minimum verify opened file/parent identities and fail closed before any sensitive bytes are written. Add deterministic race-simulation tests plus Windows junction/reparse coverage. | 2026-08-23, revalidated after BE-011 was marked DONE; residual TOCTOU remains |
| SEC-007 | P1 | Filesystem / destructive operations TOCTOU | Raw Archive deletion/cleanup paths also rely on pathname checks followed by later pathname-based hash/unlink operations. An external actor that can replace a previously validated parent directory with a link/junction between verification and `unlink()` can redirect deletion to a same-named file outside the configured storage root. The application mutation lock serializes ATHENA writers but does not exclude out-of-band filesystem mutation. | `cleanup_verified_spool_replica()` resolves/verifies paths and later executes `spool_path.unlink()`. `purge_verified_replicas()` obtains paths from `verified_replica_paths()`, re-hashes by pathname, then calls `path.unlink()`. `reconcile_orphaned_blobs()` enumerates candidates and later hashes/unlinks by pathname. Existing checks reject static symlinks but do not pin parent directory identity through destructive use. Static trace only; no destructive exploit executed. | Availability/integrity of arbitrary files reachable through a hostile parent redirection, subject to the attacker's pre-existing ability to mutate a Raw Archive ancestor. | BACKEND/SECURITY | READY | Extend the same handle/identity-safe storage primitive required by SEC-006 to destructive operations. Deletion must prove the target object's identity and containment immediately at unlink time without traversing attacker-replaceable parents. Add race-simulation regression tests for cleanup, purge and orphan reconciliation. | 2026-08-23, static delete/GC trace on current remote |
| SEC-008 | P2 | Backup target / resource exhaustion | Backup deletion-ledger reads do not enforce explicit per-file, record-count, or aggregate-byte limits before loading and decoding records. A corrupted or attacker-modified backup target can therefore force large directory materialization, large `read_bytes()` allocations and repeated JSON/canonicalization work during deletion-ledger read/validation. | `src/athena/backup/deletion_storage.py::_read_target_deletion_records()` materializes/sorts `records_root.iterdir()`, then calls `path.read_bytes()` for each accepted record; `_validate_target_deletion_head()` also uses unbounded `head_path.read_bytes()`. No maximum record count, per-record byte ceiling or aggregate ledger byte budget is enforced in the reviewed path. Static trace only; no large-file DoS executed. | Availability of backup verification/synchronization and memory/CPU consumption when a backup target is corrupt, removable, shared or attacker-modifiable. | BACKEND/SECURITY | READY | Add conservative versioned limits for deletion-ledger head bytes, individual record bytes, record count and aggregate bytes; stat files before reading, reject oversized/non-regular inputs fail-closed, and add fixtures proving pathological targets are rejected without large allocation. Keep limits comfortably above realistic retention/deletion volumes. | 2026-08-23, static backup deletion-ledger trace on current remote |
| SEC-009 | P1 | Storage migration / filesystem TOCTOU | The new clone-first migration stack rejects static symlink/junction/reparse boundaries but still performs sensitive create/replace operations by pathname after those checks. An out-of-band local actor able to replace a validated migration parent can redirect candidate creation, journal publication, cleanup or activation outside the intended directory tree. | `migration_clone.py` re-checks `candidate.parent` and then calls `sqlite3.connect(candidate)` by pathname; cleanup later unlinks candidate/sidecars by pathname. `migration_journal.py` validates the journal parent and then creates a temporary journal by pathname. `migration_activation.py` validates parents/files and then delegates source->rollback and candidate->source publication to path-based `durable_replace()`. `durable_fs.py::durable_replace()` validates parents before `os.replace()`/`MoveFileExW` but does not pin parent directory identity across the operation. Static trace only; no race exploit executed. | Integrity/confidentiality of the live database, rollback database, migration candidate and recovery journal; possible redirected creation/replacement when an attacker already has mutation access to a migration/storage ancestor. | BACKEND/SECURITY | READY | Extend BE-028's migration hardening with the same identity-bound filesystem invariant tracked by SEC-006/007. Candidate/journal creation, cleanup and activation must be tied to verified directory/file handles or equivalent OS-safe primitives, including Windows reparse-safe behavior. Add deterministic parent-replacement race tests for clone creation, journal publication, cleanup and activation. | 2026-08-23, static trace of current clone-first migration stack |
| SEC-010 | P2 | Storage migration / resource exhaustion | The external migration journal is expected to be tiny canonical JSON, but `MigrationJournalStore.load()` reads the entire journal without a file-size ceiling before UTF-8/JSON parsing. A corrupted or attacker-modified journal can therefore force disproportionate memory use during startup/recovery. | `migration_journal.py::_read_journal_file()` safely opens and verifies handle/path identity, then executes unbounded `handle.read()`. `decode_migration_journal()` parses the full byte payload. No explicit maximum journal byte size is enforced. Static trace only; no large-file DoS executed. | Availability of startup, migration recovery and administrative repair on a corrupted/hostile runtime filesystem. | BACKEND/SECURITY | READY | Define a conservative versioned maximum journal size; use `fstat()` before reading and reject oversized/non-regular files fail-closed. Keep handle/path identity verification. Add a regression proving an oversized journal is rejected without reading the full payload. | 2026-08-23, static migration-journal resource-bound review |
| SEC-011 | P1 | Local API / Windows reparse confinement | `LocalApiRuntime` previously used only `Path.is_symlink()` for runtime-root and ancestor validation, unlike the shared storage trust-boundary predicate that also recognizes Windows junctions and reparse points. An attacker-controlled junction/reparse boundary could redirect token/discovery publication outside the intended runtime tree. | `src/athena/api/runtime.py` now imports and uses `durable_fs.is_link_boundary()` for runtime-root, ancestor and file-parent checks. `tests/unit/test_api_runtime_boundaries.py` adds a deterministic reparse-boundary simulation. Fix commits `124fe5d0d5fc96a37c8bacb14879fb55908614b0` and `f1f7e60d378ba1bf677cd3a43e4472c5948cffc7`. | Confidentiality of the Core API bearer token and integrity of local discovery state on Windows. | SECURITY | FIXED | Keep shared junction/reparse predicate; obtain real targeted/Windows execution before VERIFIED. Concurrent parent replacement remains a separate residual risk under SEC-012. | 2026-08-23, static post-fix review; tests added but not executed |
| SEC-012 | P1 | Local API / token-publication TOCTOU | Even with SEC-011's static junction/reparse checks, `_write_private_text()` still validates the runtime parent and later creates the staging token/discovery file by pathname. A hostile local actor able to replace the validated parent between check and `os.open()` can redirect the write; the later re-check can detect the change only after secret token bytes may already have been written. | `src/athena/api/runtime.py::_write_private_text()` performs `_reject_symlink_ancestors()` / parent boundary checks, then `os.open(staging, O_CREAT|O_EXCL|O_NOFOLLOW)` using the full pathname, writes/fsyncs the token, and only afterwards re-checks the parent before `durable_replace()`. Static trace only; no secret-leak race executed. | Confidentiality of the ephemeral bearer token; local API trust boundary. | SECURITY | READY | Rework private runtime publication to bind the create/write/publish operation to a verified directory identity. On POSIX use dirfd/openat-style no-follow semantics where feasible; on Windows use established handle/reparse-safe APIs. Never write token bytes until parent identity is pinned. Add deterministic race-simulation coverage. | 2026-08-23, residual check/use trace after SEC-011 fix |
| SEC-013 | P1 | Storage migration / cross-process lock lifetime | The migration lock verifies the lock-file pathname against its opened handle at acquisition, but it does not pin the migration-root directory identity for the lifetime of the critical section. If an out-of-band local actor can rename/replace the migration root after process A acquires the old lock, process B can potentially create/open a different `.athena-migration.lock` at the same pathname in the replacement root and acquire a second independent OS lock. | `migration_lock.py::migration_lock()` validates `migration_root`, opens/locks `.athena-migration.lock`, re-checks only the leaf handle/path identity, then yields. No root-directory handle/identity is retained or revalidated while the critical section performs journal/clone/activation operations. Static concurrency reasoning only; no destructive race executed. | Exclusivity of schema migration; simultaneous migrations against the same logical database path could undermine clone/journal/activation safety and recovery assumptions. | BACKEND/SECURITY | READY | Extend BE-028 so migration ownership is bound to a stable root/directory identity for the full critical section. Prefer a lock anchored in an immutable/private state location or hold and validate a directory handle plus platform-equivalent identity semantics. Add a deterministic two-owner test that replaces/renames the migration root while the first lock is held and proves a second logical migration cannot enter. | 2026-08-23, static lock-lifetime trace on current remote |

## Verified baseline controls

These controls should not be re-audited without relevant code/dependency/configuration movement:

- `ExternalFetchGateway` direct mode rejects loopback/private/link-local/reserved targets and binds validated DNS results into the connection path; redirects are bounded and re-authorized.
- Tor mode delegates DNS to SOCKS, uses per-request stream-isolation credentials, constrains the proxy endpoint and fails closed rather than silently falling back to direct networking.
- Direct/Tor HTTPS paths use normal certificate and hostname validation; no permissive TLS context was found in the reviewed gateway path.
- Core API is IPv4-loopback-only, bearer-authenticated with constant-time comparison, rejects browser `Origin` requests and bounds request bodies.
- LM Studio configuration rejects non-loopback endpoint configuration; SEC-003 tracks ambient proxy handling in the adapter transport.
- Protected Content uses pyca/cryptography AES-256-GCM and Argon2id, random keys/nonces/salts and authenticated decryption; SEC-004 tracks KDF work-factor bounds.
- Native DOCX reads constrained OOXML parts without extracting archive members to filesystem paths. HTML parsing is bounded and non-executing. PDF parsing is isolated in a supervised child process with input/page/output/time/memory limits.
- Backup restore validates destination isolation, manifest/object paths, hashes and SQLite integrity before publication.
- Python/build dependencies are exact-pinned and locked with artifact hashes; SEC-005 tracks mutable GitHub Action references separately.

## Handoffs

- `SEC-001` -> RELEASE/SECURITY: prove or establish a private Windows DACL invariant for runtime token/discovery state.
- `SEC-002` -> FEATURE: decide whether plaintext HTTP remains part of the intended external-fetch contract.
- `SEC-003` -> BACKEND: make LM Studio's loopback transport explicitly proxy-free and add hostile-proxy regression coverage.
- `SEC-004` -> SECURITY: wait for real targeted/CI execution before VERIFIED.
- `SEC-005` -> QUALITY: pin executable external Actions immutably.
- `SEC-006` -> BACKEND: BE-011's static symlink/reparse protection is only a partial fix; close the check/use race for blob creation/publication using directory/object identity-safe operations.
- `SEC-007` -> BACKEND: apply the same identity-safe confinement to cleanup/purge/orphan-deletion paths; static pre-checks plus later pathname `unlink()` are insufficient against out-of-band parent replacement.
- `SEC-008` -> BACKEND: bound backup deletion-ledger resource consumption before directory materialization/file reads/JSON decoding; corrupted backup targets must fail closed without attacker-controlled memory growth.
- `SEC-009` -> BACKEND/BE-028: migration clone/journal creation, cleanup and activation need identity-bound filesystem operations; static reparse checks alone do not close parent-replacement races.
- `SEC-010` -> BACKEND/BE-028: bound migration-journal bytes before full read/JSON decode while preserving existing no-follow and handle/path identity checks.
- `SEC-011` -> QUALITY: execute targeted API-runtime boundary tests on Linux and Windows; promote only with observed green evidence.
- `SEC-012` -> SECURITY: close private token/discovery publication TOCTOU without waiting on a broad storage refactor; reuse a shared identity-safe primitive if one lands first.
- `SEC-013` -> BACKEND/BE-028: migration lock ownership must survive migration-root rename/replacement; preserve one-owner semantics across the entire critical section, not only lock-file acquisition.
