# Quality Gate runs 1658, 1705 and 1733 — Ruff progression

## Scope

Repository: `bnbgrs/pATHENA` only.

This log records observed GitHub Actions output. Results not reached by a fail-fast run are explicitly marked **NOT EXECUTED**.

## Run 1658

Workflow run `32641579945`.

- Specification validator: **PASS**, 63/63.
- Ruff: **FAIL**, four primary errors.
- mypy: **NOT EXECUTED**.
- pytest: **NOT EXECUTED**.

Observed Ruff errors:

1. `B905` in `src/athena/desktop/pathena_accessibility_flow_3000.py`: adjacent focus widgets were zipped without explicit `strict=`. Correct semantics are `strict=False` because the shifted sequence is intentionally one item shorter.
2. `I001` in `tests/unit/test_backup_deletion_codec_canonicalization.py`.
3. `I001` in `tests/unit/test_pathena_startup_experience_2900.py`.
4. `I001` in `tests/unit/test_source_model_invariants.py`.

The backup-deletion import fix landed concurrently. The accessibility and remaining test import issues were subsequently corrected on `agent/pathena`.

The same run also exposed snapshot-runtime warnings while still producing an artifact: cross-window `setTabOrder`, running `QProcess` instances destroyed during shutdown, and callbacks accessing already-deleted Qt wrappers. Those are CI snapshot-hygiene defects, separate from the Ruff primary failures.

## Run 1705

Workflow run `32641767824`.

- Specification validator: **PASS**, 63/63.
- Ruff: **FAIL**, two primary errors.
- mypy: **NOT EXECUTED**.
- pytest: **NOT EXECUTED**.

The four Ruff failures from run 1658 no longer appeared, which verifies that slice.

Observed new Ruff errors:

1. `B905` in `src/athena/desktop/pathena_focus_interaction_3100.py`.
2. `I001` in `src/athena/desktop/pathena_ui_refinement_integrity.py`.

The focus interaction file was corrected concurrently with `strict=False`. The import-integrity file continued changing as later refinement passes were added.

## Run 1733

Workflow run `32642036329`.

- Specification validator: **PASS**, 63/63.
- Ruff: **FAIL**, two primary `I001` errors.
- mypy: **NOT EXECUTED**.
- pytest: **NOT EXECUTED**.

The two run-1705 error forms were reduced to import-order failures only. Observed files:

1. `src/athena/desktop/pathena_dynamic_focus_3200.py` — PySide widget import order.
2. `src/athena/desktop/pathena_ui_refinement_integrity.py` — lexicographic module ordering after additional numbered refinement modules were added.

The dynamic-focus import was corrected concurrently. The integrity import ordering was fixed in commit `4e2a28ab41778cf313c3cdb3bf64f9e04bf8f6c3`.

## Snapshot-runtime hardening already applied during this sequence

The following teardown problems from the earlier snapshot logs were addressed independently of the Ruff gate:

- deleted `QListWidget` callback in `pathena_ui_refinement_700.py`: commit `067fdef061665914043a642e7a259cf02aaa8d93`;
- deleted Jobs list/filter callback in `pathena_jobs_experience_2800.py`: commit `f2cf75c4f1b90bdf2ee10015ef015690cda05988`;
- deleted main-window callbacks in `pathena_progressive_workspace_2300.py`: commit `ac3ab6d3027a6b741eadb66b53bb7ada45f54b7d`;
- dynamic tab-order pass made explicit and constrained to widgets in the same Qt window: commit `4a85e9da8faae6e0609fcd5e5fd3bc8aa7ab7826`.

The quality workflow itself was also restored to run the actual quality gate independently from UI snapshot rendering in commit `ebe3152bddb99307ad6a0618502acd95c2b664c0`.

## Next gate slice

The next authoritative run must establish whether Ruff is now green. Only then can mypy failures be classified. pytest remains **NOT EXECUTED** until mypy passes in the fail-fast gate.
