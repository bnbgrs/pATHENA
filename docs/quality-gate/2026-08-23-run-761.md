# Quality Gate failure log — 2026-08-23 — run 761

Repository: `bnbgrs/pATHENA`
Branch under repair: `agent/pathena`
CI run: `32629281116` (`ATHENA Quality Gate` #761)
Failing job: `Python 3.12 quality` (`97169418286`)

## Gate state before fixes

- Specification validator: PASS (`63/63`)
- Ruff: PASS
- mypy: FAIL with exactly two errors

## Error 1 — staged chunk publication call signature

Location: `src/athena/source/chunking_service.py:452`

mypy diagnostic: `Too many positional arguments for "publish_staged_build" of "SourceChunkStore" [call-arg]`.

Cause: `SourceChunkStore.publish_staged_build` declares `build_signature` as keyword-only, but `SourceChunkingService.publish_staged_default` passes it positionally. The intended API is explicitly keyword-only, so the caller must use `build_signature=build_signature` rather than broadening the store API.

Required fix: change the caller to pass `build_signature` by keyword. No behavior change is intended.

## Error 2 — source readiness return type

Location: `src/athena/desktop/sources_cli.py:122`

mypy diagnostic: `Returning Any from function declared to return "str" [no-any-return]`.

Cause: the enum/state value exposed by the job model is typed as `Any`, while `_readiness` has a strict `str` return contract.

Fix: normalize the boundary explicitly with `str(latest_job.state.value)`. This preserves emitted CLI text while satisfying the declared return type.

Fix commit: `3e6dbb9cb07d086c1cecfcad9e2a3072b762d36d`.

## Verification

Gate #765 (`32629380964`, job `97169658851`) ran on commit `04126c5377963b8c30369987b53ea7c9c92d8821` after the readiness fix and this log were committed.

Result:

- Specification validator: PASS (`63/63`)
- Ruff: PASS
- mypy: FAIL with exactly one remaining error
- `src/athena/desktop/sources_cli.py` no longer appears in mypy output, so Error 2 is verified fixed.
- The only remaining diagnostic is Error 1 at `src/athena/source/chunking_service.py:452`.

The remaining correction is intentionally kept narrow: pass `build_signature` by keyword at the existing `publish_staged_build` call, then rerun mypy and the full quality gate.
